apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: cpu-stress-train
  namespace: default
spec:
  mode: one
  selector:
    namespaces:
      - default
    labelSelectors:
      'app': 'ts-train-service'
  stressors:
    cpu:
      workers: 2
      load: 80
  duration: '10m'
